import {
  App,
  Source,
  SimpleAppOptions,
  SimpleSourceOptions,
} from "@grouparoo/core";

export async function sourcePreview(
  app: App,
  appOptions: SimpleAppOptions,
  source: Source,
  sourceOptions: SimpleSourceOptions
) {
  return []; // no preview at the source level
}
