exports.default = async function buildConfig() {
  return [
    {
      class: "app",
      id: {{{id}}},
      name: {{{id}}},
      type: "{{{__pluginName}}}",
      options: {
        apiKey: "...", // Braze api key
        restEndpoint: "...", // Braze rest endpoint
      }
    },
  ];
};
