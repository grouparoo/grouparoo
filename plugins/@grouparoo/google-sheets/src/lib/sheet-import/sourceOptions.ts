import { App, SimpleAppOptions } from "@grouparoo/core";

// TODO: be nice to have signature of what this should return
// TODO: can this be optional in the plugin declaration if nothing?
export async function sourceOptions(app: App, appOptions: SimpleAppOptions) {
  return {};
}
