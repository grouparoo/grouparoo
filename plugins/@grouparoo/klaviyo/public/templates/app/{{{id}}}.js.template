exports.default = async function buildConfig() {
  return [
    {
      class: "app",
      id: {{{id}}},
      name: {{{id}}},
      type: "{{{__pluginName}}}",
      options: {
        privateToken: "..." // Klaviyo Private token
        publicToken: "..." // Klaviyo Public token
      }
    },
  ];
};
