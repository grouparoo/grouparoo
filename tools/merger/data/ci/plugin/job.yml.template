  {{{job_name}}}:
    docker:
      - image: circleci/node:12
      - image: redis:latest
      - image: circleci/postgres:9
        environment:
          POSTGRES_PASSWORD: password
    steps:
      - checkout
      - restore_cache:
          <<: *cache-options  
{{{setup_steps}}}
      - run:
          name: test
          command: cd {{{relative_path}}} && npm run test


mysql has additional step after postgres
